plugins {
    id 'java-library'
}

bootJar { enabled = false }
jar { enabled = true }

dependencies {
    api project(':common:core')
    // add firebase-admin if implementation exists in the future
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

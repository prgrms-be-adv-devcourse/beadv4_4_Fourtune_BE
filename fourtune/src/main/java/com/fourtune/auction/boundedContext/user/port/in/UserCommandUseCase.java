package com.fourtune.auction.boundedContext.user.port.in;

/**
 * User Command Use Case (Inbound Port)
 * 사용자 명령 처리 인터페이스
 */
public interface UserCommandUseCase {
    
    // TODO: Use Case 메서드 정의
    // UserInfo createUser(UserCreateCommand command);
    // void updateUser(Long userId, UserUpdateCommand command);
    // void deleteUser(Long userId);
}

